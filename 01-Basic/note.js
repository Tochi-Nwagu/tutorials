/**
 * Strings are a sequence of characters enclosed in single or double quotes.
 * Example: 'Hello', "Hello", `Hello`
 * They are immutable, meaning their value cannot be changed after they are created.
 * They are indexed, meaning each character has a position in the string.
 * They are iterable, meaning they can be looped through using a for loop.
 * They are case-sensitive, meaning 'Hello' and 'hello' are different.
 * They are immutable, meaning their value cannot be changed after they are created.
 * They are indexed, meaning each character has a position in the string.
 * They are iterable, meaning they can be looped through using a for loop.
 * They are case-sensitive, meaning 'Hello' and 'hello' are different.
 * 
 * Strings can also be enclosed in backticks to allow for string interpolation.
 * Example: `Hello ${name}`
 * This can also be called template literals.
 * Template literals are a way to create strings that are more readable and easier to write.
 * Template literals can be used to embed expressions within strings.
 * Template literals can also be used to create multi-line strings.
 * Example:
 * const message = `
 * Hello ${name}
 * Welcome to the website
 * `
 */

/**
 * Taking Decisions IF/Else Statements
 * Example:
 * if (condition) {
 *  // code to execute if condition is true
 * } else {
 *  // code to execute if condition is false
 * }
 * 
 * This is called a controlled structure
 */

/**
 * TYPE CONVERSION AND TYPE COERCION
 * type conversion is when we manually convert from one data type to another.
 * Type Coercion is when JavaScript automatically converts types behind the scene for us.
 * 
 * Type Conversion
 * We can do this by using the inbuilt method of Number / string
 * 
 * //Type Coercion
 * the addition operator does automatically convert numbers to strings where they are added to strings
 */